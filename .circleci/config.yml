version: 2
jobs:
  build:
    docker: 
      - image: circleci/node:8.11-browsers
    steps:
      - checkout
      - run: 
          name: Install Dependencies
          command: |
            cd react
            yarn install
            cd ../angular
            yarn install
            cd ..
              
            
      - run:
          name: Test component's utility functions
          command: |
            cd react
            yarn test
            cd ../angular
            yarn test